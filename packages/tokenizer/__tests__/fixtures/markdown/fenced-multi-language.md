# Multi-Language Example

JavaScript:

```js
const x = 1;
const y = 2;
const z = 3;
```

CSS:

```css
.foo { color: red; margin: 0; padding: 0; }
.bar { color: blue; margin: 0; padding: 0; }
.baz { color: green; margin: 0; padding: 0; }
```

Python:

```python
x = 1
y = 2
z = 3
```
